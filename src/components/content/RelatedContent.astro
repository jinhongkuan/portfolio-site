---
import RelatedContentSvelte from './RelatedContent.svelte';

export interface Props {
  notes?: Array<{title: string, url: string}>;
  threads?: Array<{title: string, url: string}>;
}

const { notes = [], threads = [] } = Astro.props;
---

<RelatedContentSvelte 
  client:visible
  notes={notes}
  threads={threads}
/>