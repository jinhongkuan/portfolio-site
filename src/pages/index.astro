---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageTransition from '../components/layout/PageTransition.svelte';
---

<BaseLayout>
  <PageTransition client:load>
    <div class="homepage">
    <h1 class="site-title">JIN KUAN</h1>

    <div class="sections">
      <section class="section-item">
        <h2>Practice</h2>
        <p>Helping Seed-stage startups bridge business intent and complex engineering through flexible, hands-on and consultative engagement.</p>
        <p>I've led engineering as a Seed-stage CTO and shipped production systems in healthtech and fintech, guiding feature design and roadmapping through pivots and productization.</p>
        <p class="cta"><a href="/portfolio-site/practice">Schedule an intro call →</a></p>
      </section>
      
      <section class="section-item">
        <h2>Field Notes</h2>
        <p>Reflections from practical observations and readings, centered on a question often hurriedly brushed over - <a href="/portfolio-site/about">does technology actually create freer societies?</a></p>
      </section>
    </div>
    
    <nav class="home-nav">
      <a href="/portfolio-site/field-notes">Field Notes</a>
      <span class="separator">·</span>
      <a href="/portfolio-site/threads">Threads</a>
    </nav>
    </div>
  </PageTransition>
</BaseLayout>

<style>
  .homepage {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: calc(100vh - 12rem);
    text-align: center;
    max-width: 720px;
    margin: 0 auto;
  }

  .site-title {
    font-size: 2.22rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    margin-bottom: 4rem;
    line-height: 1;
  }

  .sections {
    margin: 3rem 0;
    text-align: left;
    width: 100%;
    max-width: 680px;
  }

  .section-item {
    margin-bottom: 2.5rem;
  }

  .section-item h2 {
    font-family: var(--font-sans);
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .section-item p {
    font-size: 1rem;
    line-height: 1.6;
    margin: 0 0 0.75rem 0;
  }

  .section-item p:last-child {
    margin-bottom: 0;
  }

  .section-item .cta {
    margin-top: 1rem;
  }

  .section-item .cta a {
    font-family: var(--font-sans);
    font-weight: 500;
    color: var(--color-accent);
    text-decoration: none;
    display: inline-block;
    transition: transform 0.2s ease;
  }

  .section-item .cta a:hover {
    transform: translateX(3px);
  }

  .home-nav {
    font-family: var(--font-sans);
    font-size: 1rem;
    margin-top: 3rem;
  }

  .home-nav a {
    color: var(--color-text);
    text-decoration: none;
    opacity: 0.7;
    transition: opacity 0.2s ease;
    margin: 0 1rem;
  }

  .home-nav a:hover {
    opacity: 1;
  }

  .separator {
    opacity: 0.3;
  }

  @media (max-width: 768px) {
    .site-title {
      font-size: 2rem;
    }
  }
</style>